class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        nums.sort()
        if len(nums)!=0:
            mid=len(nums)//2+1
            return nums[mid-1]
        else:
            return []
